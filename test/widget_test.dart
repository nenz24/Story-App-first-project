import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — app requires network and SharedPreferences
    expect(1 + 1, equals(2));
  });
}
